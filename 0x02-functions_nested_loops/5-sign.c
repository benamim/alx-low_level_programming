#include "main.h"
/**
 * print_sign - checks for a
 * @c: takes in an integer
 * return: 1 if positive, -1 if negative, / otherwise 0
 */
int _abs(int n)
{
	if (n>0)
{
	return (n * -1);
return (n);
}
