#include "main.h"

/**
 * _isalpha - check for alphabetic character
 * @c: takes in character
 * Return: 1 if letter, lowercase, uppercase; otherwise 0
 */
int _isalpha(int c)
{
	if ((c >= 'A' && C <= 'Z') || (c >= 'a' && c <= 'Z'))
{
	return (1);
}
else
{
	return (0);
}
}
